#account-page.account-page
  = render "devise/shared/header" 
  .main
    .upper-info
      %p 会員情報登録
    .lower-info
      .user-form
        = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
          = devise_error_messages!
          .field
            .field-label
              = f.label :ニックネーム
              %span.form-require
                必須
            .field-input
              = f.text_field :nickname, autofocus: true, placeholder: "(例)カリメロ"
          .field
            .field-label
              = f.label :メールアドレス
              %span.form-require
                必須
            .field-input
              = f.email_field :email, placeholder: "PC・携帯どちらでも可"
          .field
            .field-label
              = f.label :パスワード
              %span.form-require
                必須
            .field-input
              = f.password_field :password, autocomplete: "off", placeholder: "７文字以上の半角英数字"
          %p.form-info-text
            ※英字と数字の両方を含めて設定してください   
          %br
          %h3.chapter-sub-head
            本人確認
          %p.identification-description
            安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
          .field__multi.field___multi--character
            .field-label
              = f.label :お名前（全角）
              %span.form-require
                必須
            .field-input--name
              = f.text_field :last_name, autofocus: true            
            .field-input--name
              = f.text_field :first_name, autofocus: true       
          .field__multi.field__multi--kana
            .field-label
              = f.label :お名前カナ（全角）
              %span.form-require
                必須
            .field-input--name
              = f.text_field :kana_last_name, autofocus: true            
            .field-input--name
              = f.text_field :kana_first_name, autofocus: true         
          -# .field
          -#   .field-label
          -#     = f.label :生年月日
          -#     %span.form-require
          -#       必須
          -#   .field-input
          -#     = f.text_field :birthday, autofocus: true
             





          -# .field
          -#   .field-label
          -#     = f.label :お名前カナ（全角）
          -#     %span.form-require
          -#       必須
          -#   .field-input
          -#     = f.text_field :name, autofocus: true            
          -# .field
          -#   .field-label
          -#     = f.label :生年月日
          -#     %span.form-require
          -#       必須
          -#   .field-input
          -#     = f.text_field :name, autofocus: true
          .form-info-text
            ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          %br
          .form-info-text
            「次へ進む」のボタンを押すことにより、利用規約に同意したものとみなします
          .actions
            = f.submit "次へ進む", class: 'btn'
          %p
            本人情報の登録について
  = render "devise/shared/footer"
